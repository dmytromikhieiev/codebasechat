services:
  api:
    volumes:
      - ./secrets/github_app_private_key.pem:/run/secrets/github_app_private_key.pem:ro

  worker:
    volumes:
      - ./secrets/github_app_private_key.pem:/run/secrets/github_app_private_key.pem:ro